# Shared skills

Skills live here. Sync creates one symlink per top-level name under `.cursor/skills/` and `.claude/skills/` into this tree. Add other skills directly under those tool folders in the target repo; sync does not remove them.

Add each bundled skill as `skill-name/SKILL.md` per tool conventions.
