# /create-idea

The user invoked the **create-idea** workflow.

**Do this:** Read and follow **`.cursor/skills/create-idea/SKILL.md`** from start to finish. That file is the single source of truth (steps, git/gh commands, conventions).

**Context from the user:** Everything after `/create-idea` in their message (quoted idea, stack hints, constraints). Use it in step 1 (clarify) and step 2 (plan).

If the idea text is empty, ask what they want to build, then continue with the skill.
