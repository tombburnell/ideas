services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: snake
      POSTGRES_PASSWORD: snake
      POSTGRES_DB: snake
    ports:
      - "5432:5432"
    volumes:
      - snake_pgdata:/var/lib/postgresql/data

volumes:
  snake_pgdata:
